# Environment Variables Example
# Copy this file to .env and fill in your values
# Task Reference: T026 - Feature: 001-project-init-architecture

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Neon Serverless PostgreSQL connection string
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-example.region.aws.neon.tech/neondb?sslmode=require

# =============================================================================
# AUTHENTICATION
# =============================================================================
# Task Reference: T015 - Feature: 002-auth-identity
#
# CRITICAL: BETTER_AUTH_SECRET and JWT_SECRET MUST be identical!
#
# How JWT authentication works in this app:
# 1. Frontend (Better Auth) signs JWTs with BETTER_AUTH_SECRET using HS256
# 2. Backend (FastAPI) verifies JWTs with JWT_SECRET using HS256
# 3. If secrets don't match, token verification fails with 401 errors
#
# Requirements:
# - Minimum 32 characters (enforced by backend validation)
# - Use a cryptographically secure random string
# - Never commit actual secrets to version control
#
# Generate a secure secret with:
#   openssl rand -base64 32
#   # or
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
BETTER_AUTH_SECRET=your-secret-key-change-in-production-min-32-chars

# JWT configuration (used by backend for token verification)
# Uses variable reference to ensure secrets stay synchronized
JWT_SECRET=${BETTER_AUTH_SECRET}
JWT_ALGORITHM=HS256

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
# Environment mode: development | staging | production
ENVIRONMENT=development

# Backend server settings
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# CORS allowed origins (comma-separated for multiple)
CORS_ORIGINS=http://localhost:3000

# =============================================================================
# FRONTEND CONFIGURATION (NEXT_PUBLIC_ prefix exposes to browser)
# =============================================================================
# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Frontend app URL (for Better Auth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# PUSH NOTIFICATIONS (006-recurring-reminders)
# =============================================================================
# VAPID keys for Web Push notifications
# Generate with: python -c "from pywebpush import webpush; keys = webpush.generate_vapid_keypair(); print(f'VAPID_PUBLIC_KEY={keys[\"public_key\"]}\\nVAPID_PRIVATE_KEY={keys[\"private_key\"]}')"
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_CLAIMS_EMAIL=mailto:admin@yourdomain.com

# =============================================================================
# OPTIONAL: DEVELOPMENT HELPERS
# =============================================================================
# Enable debug logging
DEBUG=false

# Database connection pool settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
    